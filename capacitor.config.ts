import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Acreditodo.app',
  appName: 'Acreditodo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
